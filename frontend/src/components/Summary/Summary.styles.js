import styled from 'styled-components';

export const SummaryWrapper = styled.div`
    display: flex;
    align-items: center;
    max-width: var(--maxWidth);
    padding: 0 20px;
    margin: 0 auto;
    border-style: solid;
    border-radius:10px;
    background: white;
`;